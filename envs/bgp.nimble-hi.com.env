# env file for bgp.nimble-hi.com (client / verification host)
# This host does not announce prefixes, so no BGP session is configured.
# File is here for completeness and east/west route documentation.
#
# East/west static routes to install on this host:
#   sudo ip route add 204.14.240.0/22 via <BIRD_HOST_PUBLIC_IPV4>
# See etc/systemd/network/20-east-west.network for a persistent version.
